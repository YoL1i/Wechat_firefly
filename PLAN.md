# WChat Agent - 项目计划书

> 一个住在微信里的 AI 角色

---

## 一、项目概述

WChat Agent 是一个基于个人微信的 AI 智能体。它不是工具，而是一个 **有性格、有情感、会主动找你说话的角色**。你可以把它自定义成你喜欢的游戏角色。

核心理念：角色驱动 | 情感真实 | 主动存在 | 声音人格 | 时刻在线

---

## 二、功能全景

- **角色系统** - 自定义游戏角色，配置性格、口癖、背景故事、声音风格，支持多角色卡切换
- **智能对话** - LLM 驱动（DeepSeek/OpenAI），带角色扮演和上下文记忆
- **语音消息** - Edge-TTS 起步，角色风格化说话，预留声音克隆接口
- **情感引擎** - 开心→失落→生气→委屈，真实时间线，影响对话语气
- **主动消息** - 随机找你聊天，情感状态决定语气
- **定时提醒** - 喝水、学习等自定义提醒
- **日程管理** - 记录和提醒日程
- **每日简报** - 早安推送：天气+新闻+今日安排
- **随手记** - 记录碎片信息，随时查询
- **学习追踪** - 记录学习内容和时长，督促进度
- **趣味互动** - 角色专属彩蛋、冷笑话



---

## 三、技术架构

### 开发阶段（本地单进程）

所有模块在本地一个进程运行，架构按分层写，迁移到服务器只需改配置。

wchat/
  +-- bridge/     # 微信接入层（WeChatFerry）
  +-- core/
  |   +-- brain.py      # AI 对话引擎
  |   +-- emotions.py   # 情感引擎
  |   +-- scheduler.py  # 调度中心
  |   +-- memory.py     # 记忆系统
  +-- services/
  |   +-- tts.py         # 语音
  |   +-- reminders.py   # 提醒
  |   +-- initiative.py  # 主动消息
  +-- config/     # 配置加载
  +-- main.py     # 入口

### 生产阶段（混合架构）

服务器（Ubuntu / 1核2G）：brain + emotions + scheduler + memory + WebSocket Server
本地 Windows：微信客户端 + WeChatFerry + 轻量桥接程序

通信方式：WebSocket（心跳+消息转发）

### 技术栈

Python 3.12 | WeChatFerry | DeepSeek API | Edge-TTS | APScheduler | SQLite | WebSocket | loguru



---

## 四、角色卡系统

角色卡是一个 JSON 文件，定义角色的全部人格。

```json
{
  "name": "角色名称",
  "from": "作品出处",
  "personality": {
    "traits": ["活泼", "调皮"],
    "voice": {"engine": "edge-tts"}
  }
}
```

支持多角色卡，通过配置或指令切换。

---

## 五、情感引擎

状态机：开心 ↔ 失落 ↔ 生气 ↔ 委屈

- 开心：正常互动维持
- 失落：冷落 4h+ 触发，主动问候频率上升
- 生气：冷落 12h+ 触发，对话语气明显变化
- 委屈：被喵后回复过渡态，好好喵就回开心

情感影响：对话语气 | 主动消息内容 | 语音语调（预留）

---

## 六、开发阶段

Phase 1 - 基础骨架 [当前] | 目录结构 + 角色卡 + 配置 + Git
Phase 2 - 微信接入 | WeChatFerry 消息收发
Phase 3 - AI 对话 | DeepSeek API + 角色扮演 + 上下文
Phase 4 - 情感引擎 | 状态机 + 冷落检测 + 情感影响
Phase 5 - 语音 | Edge-TTS + 角色风格化语音
Phase 6 - 主动消息&提醒 | APScheduler + 定时提醒 + 随机消息
Phase 7 - 扩展功能 | 随手记 + 学习追踪 + 每日简报 + 日程
Phase 8 - 混合架构部署 | server/bridge 拆分 + WebSocket + 部署

---

## 七、部署规划

服务器：Linux 1核2G，Python 3.12+ systemd 守护
本地：Windows 微信客户端 + 桥接程序开机自启

---

## 八、风险与对策

| 风险 | 对策 |
|---|---|
| 微信被封号 | 控制频率，模拟正常使用 |
| API Key 泄露 | 环境变量管理，服务端只读 |
| 服务器断网 | 桥接自动重连，上下文不丢失 |
| 电脑关机 | 微信离线，设计可长期后台运行 |

---

> 本计划书随开发推进持续更新。
